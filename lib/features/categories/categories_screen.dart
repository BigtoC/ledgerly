// TODO(M5): Categories management screen — list grouped by expense/income,
// add / edit / reorder / archive, subcategory management. Used categories
// are archived, not deleted.

import 'package:flutter/material.dart';

class CategoriesScreen extends StatelessWidget {
  const CategoriesScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Categories')));
}
