// TODO(M5): `SettingsController` — reads and writes through
// `UserPreferencesRepository`. Splash settings (`splash_enabled`,
// `splash_start_date`, `splash_display_text`, `splash_button_label`) are
// part of this surface.
