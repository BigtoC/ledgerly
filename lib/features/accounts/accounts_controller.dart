// TODO(M5): `AccountsController` — derives balances in each account's
// native currency from `TransactionRepository`. Summaries stay grouped by
// currency in MVP; Phase 2 adds auto-converted totals in `default_currency`.
