// TODO(M5): `TransactionFormController` — owns form state and save /
// duplicate commands. Prefills from an existing transaction when invoked
// via the Quick Repeat flow (Home owns the affordance; this controller
// owns the prefill + save semantics).
