// TODO(M1): Thin wrapper around `Platform.localeName` so bootstrap can
// seed `default_currency` from the actual device locale from day 1.
//
// Expanded in M6: explicit BCP 47 parsing, test-friendly locale override.
