// TODO(M1): Drift table for `transactions` per PRD -> Database Schema.
// `amount_minor_units` is stored as `int` (see Money Storage Policy);
// transaction type (expense/income) is derived from the linked category's
// `type` column.
