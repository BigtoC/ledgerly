// TODO(M1): Drift table for `categories` per PRD -> Database Schema.
// Stores `l10n_key` for seeded rows, `custom_name` for user renames,
// `icon` as a registry key, and `color` as a palette index.
