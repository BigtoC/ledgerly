// TODO(M1): Drift table for `user_preferences` per PRD -> Database Schema.
// Key/value store. Values are JSON-encoded text. Used for theme, locale,
// default account / currency, first-run state, and splash settings
// (`splash_enabled`, `splash_start_date`, `splash_display_text`,
// `splash_button_label`).
