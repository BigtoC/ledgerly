// TODO(M1): Drift table for `currencies` per PRD -> Database Schema.
// Single source of truth for currency decimals (2 for USD, 0 for JPY,
// 18 for ETH). Other tables reference `currencies.code` as a FK.
